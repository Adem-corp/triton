name: Deploy test website on push

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Upload files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.TEST_FTP_HOST }} #TODO заменить данные на production
          username: ${{ secrets.TEST_FTP_USER }}
          password: ${{ secrets.TEST_FTP_PASSWORD }}
          local-dir: ./
          server-dir: /www/adem77.ru/wp-content/themes/triton/
